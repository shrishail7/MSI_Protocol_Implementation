

```
┌──────────┐                          ┌──────────┐
│ PROC P1  │                          │ PROC P2  │
└────┬─────┘                          └─────┬────┘
     │ proc_addr[15:0]                      │
     │ proc_data[127:0]                     │
     │ proc_rd_wr, proc_valid               │
     │ proc_ack                             │
     │                                      │
┌────▼──────────────────────────────────────▼────┐
│  CACHE_CONTROLLER_P1        CACHE_CONTROLLER_P2 │
│  (L1_1 Interface)           (L1_2 Interface)    │
└────┬─────────────────────────────────────┬──────┘
     │                                     │
     │ bus_req_valid                       │
     │ bus_req_type[1:0]                   │
     │ bus_req_addr[15:0]                  │
     │ bus_data[127:0]                     │
     │                                     │
┌────▼─────────────────────────────────────▼────┐
│         BUS_ARBITOR / CONTROLLER              │
│  (Snooping & Arbitration Logic)               │
└────┬──────────────────────────────────┬───────┘
     │                                  │
     │ snoop_invalidate                 │
     │ snoop_addr[15:0]                 │
     │                                  │
     │ l2_req_valid                     │
     │ l2_req_addr[15:0]                │
     │ l2_req_type[1:0]                 │
     │                                  │
┌────▼──────────────────────────────────▼──────┐
│      L2_CACHE_CONTROLLER                     │
│   (8KB, 2-way, write-through to memory)      │
└────┬────────────────────────────────────┬────┘
     │                                    │
     │ mem_req_valid                      │
     │ mem_req_addr[15:0]                 │
     │ mem_req_type, mem_data[127:0]      │
     │ mem_ack                            │
     │                                    │
┌────▼────────────────────────────────────▼───┐
│       MEMORY_CONTROLLER                     │
│    (64KB Main Memory)                       │
└─────────────────────────────────────────────┘
```